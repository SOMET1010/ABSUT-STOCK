<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="ansut_distribution_view_form_withdrawal" model="ir.ui.view">
        <field name="name">ansut.distribution.form.withdrawal</field>
        <field name="model">ansut.distribution</field>
        <field name="inherit_id" ref="ansut_distribution.ansut_distribution_view_form"/>
        <field name="arch" type="xml">
            <header position="inside">
                <button name="action_print_withdrawal_report" type="object"
                        string="PV de remise" invisible="state != 'closed'"/>
            </header>
        </field>
    </record>

</odoo>
